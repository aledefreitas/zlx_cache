language: php

dist: trusty

php:
    - 7.0

sudo: required

services:
    - memcache
    - redis-server

before-install:
  - phpenv config-rm xdebug.ini
  - composer self-update

install:
    - yes '' | pecl install -f redis
    - yes '' | pecl install -f memcached
    - composer install

script:
    - vendor/bin/phpunit
